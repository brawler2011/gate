openapi: 3.0.3
info:
  title: ObserverService API
  version: 0.0.1
paths:
  /submissions:
    get:
      operationId: ObserveSubmissions
      parameters:
        - name: since
          in: query
          required: true
          schema:
            type: integer
            format: int64
        - name: contestId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: userId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: problemId
          in: query
          required: false
          schema:
            type: string
            format: uuid
        - name: language
          in: query
          required: false
          schema:
            type: integer
            format: int32
      responses:
        "101":
          description: Switching protocols
  /contests:
    get:
      operationId: ObserveContests
      parameters:
        - name: since
          in: query
          required: false
          schema:
            type: integer
            format: int64
        - name: contestId
          in: query
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "101":
          description: Switching protocols
components:
  schemas:
    SubmissionsWsCloseCode:
      type: integer
      format: int32
      enum:
        - 4408
        - 4409
        - 4410
      x-enum-varnames:
        - buffer_empty
        - history_lost
        - invalid_range
    SubmissionsWsCloseReason:
      type: string
      enum:
        - buffer_empty
        - history_lost
        - invalid_range
    SubmissionEventMeta:
      type: object
      properties:
        user_id:
          type: string
          format: uuid
        username:
          type: string
        contest_id:
          type: string
          format: uuid
        contest_title:
          type: string
        problem_id:
          type: string
          format: uuid
        problem_title:
          type: string
        position:
          type: integer
          format: int32
        language:
          type: integer
          format: int32
        created_at:
          type: string
          format: date-time
    SubmissionsEventType:
      type: string
      enum:
        - submissions.created
        - submissions.queued
        - submissions.compiling_started
        - submissions.testing_started
        - submissions.test_started
        - submissions.completed
    SubmissionsMessage:
      type: object
      required:
        - event_type
        - payload
      properties:
        event_type:
          $ref: "#/components/schemas/SubmissionsEventType"
        payload:
          oneOf:
            - $ref: "#/components/schemas/MessageSubmissionCreated"
            - $ref: "#/components/schemas/MessageSubmissionQueued"
            - $ref: "#/components/schemas/MessageSubmissionCompilingStarted"
            - $ref: "#/components/schemas/MessageSubmissionTestingStarted"
            - $ref: "#/components/schemas/MessageSubmissionTestStarted"
            - $ref: "#/components/schemas/MessageSubmissionCompleted"
    MessageSubmissionCreated:
      allOf:
        - $ref: "#/components/schemas/SubmissionEventMeta"
        - type: object
          required:
            - id
            - state
            - source
          properties:
            id:
              type: string
              format: uuid
            state:
              type: integer
              format: int32
            source:
              type: string
    MessageSubmissionQueued:
      allOf:
        - $ref: "#/components/schemas/SubmissionEventMeta"
        - type: object
          required:
            - id
          properties:
            id:
              type: string
              format: uuid
    MessageSubmissionCompilingStarted:
      allOf:
        - $ref: "#/components/schemas/SubmissionEventMeta"
        - type: object
          required:
            - id
          properties:
            id:
              type: string
              format: uuid
    MessageSubmissionTestingStarted:
      allOf:
        - $ref: "#/components/schemas/SubmissionEventMeta"
        - type: object
          required:
            - id
          properties:
            id:
              type: string
              format: uuid
    MessageSubmissionTestStarted:
      allOf:
        - $ref: "#/components/schemas/SubmissionEventMeta"
        - type: object
          required:
            - id
            - number
          properties:
            id:
              type: string
              format: uuid
            number:
              type: integer
              format: int32
    MessageSubmissionCompleted:
      allOf:
        - $ref: "#/components/schemas/SubmissionEventMeta"
        - type: object
          required:
            - id
            - state
            - score
            - penalty
            - time_stat
            - memory_stat
          properties:
            id:
              type: string
              format: uuid
            state:
              type: integer
              format: int32
            score:
              type: integer
              format: int32
            penalty:
              type: integer
              format: int32
            time_stat:
              type: integer
              format: int32
            memory_stat:
              type: integer
              format: int32
            failed_test:
              type: integer
              format: int32
              nullable: true
    ContestEventType:
      type: string
      enum:
        - contest.announcement_created
        - contest.announcement_deleted
        - contest.clarification_created
        - contest.clarification_answered
    ContestMessage:
      type: object
      required:
        - event_type
        - payload
      properties:
        event_type:
          $ref: "#/components/schemas/ContestEventType"
        payload:
          oneOf:
            - $ref: "#/components/schemas/MessageContestAnnouncementCreated"
            - $ref: "#/components/schemas/MessageContestAnnouncementDeleted"
            - $ref: "#/components/schemas/MessageContestClarificationCreated"
            - $ref: "#/components/schemas/MessageContestClarificationAnswered"
    MessageContestAnnouncementCreated:
      type: object
      required:
        - id
        - contest_id
        - title
        - body
        - author_username
        - created_at
      properties:
        id:
          type: string
          format: uuid
        contest_id:
          type: string
          format: uuid
        problem_id:
          type: string
          format: uuid
        problem_title:
          type: string
        problem_letter:
          type: string
        author_id:
          type: string
          format: uuid
        author_username:
          type: string
        title:
          type: string
        body:
          type: string
        created_at:
          type: string
          format: date-time
    MessageContestAnnouncementDeleted:
      type: object
      required:
        - id
        - contest_id
      properties:
        id:
          type: string
          format: uuid
        contest_id:
          type: string
          format: uuid
    MessageContestClarificationCreated:
      type: object
      required:
        - id
        - contest_id
        - user_id
        - username
        - question
        - created_at
      properties:
        id:
          type: string
          format: uuid
        contest_id:
          type: string
          format: uuid
        problem_id:
          type: string
          format: uuid
        problem_title:
          type: string
        problem_letter:
          type: string
        user_id:
          type: string
          format: uuid
        username:
          type: string
        question:
          type: string
        created_at:
          type: string
          format: date-time
    MessageContestClarificationAnswered:
      type: object
      required:
        - id
        - contest_id
        - user_id
        - username
        - question
        - answer
        - answered_at
      properties:
        id:
          type: string
          format: uuid
        contest_id:
          type: string
          format: uuid
        problem_id:
          type: string
          format: uuid
        problem_title:
          type: string
        problem_letter:
          type: string
        user_id:
          type: string
          format: uuid
        username:
          type: string
        question:
          type: string
        answer:
          type: string
        answered_by:
          type: string
          format: uuid
        answered_by_username:
          type: string
        answered_at:
          type: string
          format: date-time

