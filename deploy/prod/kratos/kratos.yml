version: v1.3.1

log:
  level: info
  format: json
  leak_sensitive_values: false

serve:
  public:
    base_url: https://gate149.ru/
    cors:
      enabled: true
      allowed_origins:
        - https://gate149.ru
      allowed_methods:
        - GET
        - POST
        - PUT
        - PATCH
        - DELETE
      allowed_headers:
        - Authorization
        - Content-Type
        - Cookie
        - Origin
        - Credentials
      exposed_headers:
        - Set-Cookie
        - Content-Type
      allow_credentials: true
      debug: false
  admin:
    base_url: https://gate149.ru/kratos/admin/

session:
  cookie:
    same_site: Lax
    domain: gate149.ru
    path: /
    secure: true
    persistent: true

identity:
  default_schema_id: default
  schemas:
    - id: default
      url: file:///etc/config/kratos/identity.schema.json

selfservice:
  default_browser_return_url: https://gate149.ru/
  allowed_return_urls:
    - https://gate149.ru/
    - /
    - /auth/login
  flows:
    error:
      ui_url: /error
    login:
      ui_url: /auth/login
    registration:
      ui_url: /auth/registration
      after:
        password:
          hooks:
            - hook: session
            - hook: web_hook
              config:
                url: http://backend:8081/webhook/kratos
                method: POST
                body: file:///etc/config/kratos/webhook_body.jsonnet
                response:
                  ignore: true
                headers:
                  Content-Type: application/json
    verification:
      ui_url: /auth/verification
    recovery:
      ui_url: /auth/recovery
    settings:
      ui_url: /settings
    logout:
      after:
        default_browser_return_url: /auth/login

  methods:
    password:
      enabled: true
