name: Deploy Development

on:
  workflow_dispatch:

jobs:
  deploy:
    uses: ./.github/workflows/deploy.yml
    with:
      environment: dev
      domain_name: dev.steins.ru
      deploy_dir: ~/gate/deploy/dev
    secrets: inherit
